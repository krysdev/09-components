// This file is used to sort data rendered by Table.js and displayed through TablePage.js

// As an input for the Table.js component, so it can sort, we have:
// - an array of objects ('data' hardcoded in TablePage)
// - a function to extract a value we sort by
// - sorting order (ascending or descending)

function SortableTable() {
  
  return (
    <div>
      
    </div>
  );
}

export default SortableTable